# HufiAgents V1.1.0

## Release scope

- Dynamic agents, persistent messaging, delegation and fan-out/fan-in.
- Routines, controlled workspaces/sessions and connector registry.
- Chat-first UI with real agent and routine creation, approval cards and `/legacy`.
- Read-only HufManager team mission through the existing local Qwen router,
  independent reviewer and audit trail.
- Release gates: routine intent confirmation, refusal-aware review and
  reachable tool-call approvals.

## Validation and deployment

- CI passed for the final integration commit. Claude's release-gate validation
  reports 282 tests passed, clean Ruff/format/compile and a real browser pass
  at seven breakpoints; earlier XXL validation passed 273 tests including
  Bubblewrap, loopback, TestClient/lifespan and migrations.
- PR #14 merged to `main` as `71212bdee9599d4354d4f01753c5a6703c59e3e0`.
- Production SQLite backup: `run/hufiagents-v1.0.1-pre-v1.1.0-20260908.sqlite3`
  (owner `hufiagents:hufifactory`, mode `0600`). Upgrade applied migrations
  `002 -> 003 -> 004`, preserving 6 missions and 161 audit events.
- `hufiagents.service` restarted successfully; loopback and HTTPS health are
  healthy at `https://agents.heyhufi.com`.

## Honest limit

Computer/browser sessions are a prepared foundation. A persistent desktop is
not shipped in V1.1.0.
