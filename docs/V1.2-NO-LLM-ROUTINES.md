# V1.2 No-LLM Routines

`deterministic_check` supports HTTP status, disk usage, file existence, and
Git cleanliness. It returns structured facts without invoking a model. A
caller may persist healthy results as Work Evidence and escalate only an
anomaly to the local routing policy.
