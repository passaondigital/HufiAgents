# V1.2 Credential Foundation

Credential handles store connector, label, scopes, and lifecycle metadata
only. Registration accepts no secret value. Normal reads therefore cannot
leak plaintext credentials; rotation and revocation are metadata operations
and audit events contain only IDs and connector names. The secure connector
implementation remains an integration boundary and must fail closed when no
encrypted store is configured.
